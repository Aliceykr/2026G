# 2026 G题交付验证记录

最后更新：`2026-07-29 19:21:30 +08:00`

## 1. 自动化验证

| 验证项 | 结果 | 关键证据 |
|---|---|---|
| 频谱主机测试 | 通过 | 无信号、10k～500k、非整bin、谐波、spur |
| 测量主机测试 | 通过 | 7点标定、插值、1/3周期、端到端误差 |
| Keil | 通过 | `0 Error(s), 0 Warning(s)` |
| STM32程序尺寸 | 通过 | Code 21318，RO 120746，RW 68，ZI 54716 |
| GPIO审计 | 通过 | 冲突0 |
| 栈审计 | 通过 | 配置4096字节，最大静态路径256字节 |
| 启动路径审计 | 通过 | 仅default一条路径 |
| ModelSim | 通过 | `PASS: g_filter_precision`，0 errors/warnings |
| Quartus | 通过 | 完整编译0 errors，SOF已生成 |
| 时序 | 通过已分析路径 | 全部TNS=0，setup最小2.753ns，hold最小0.112ns |

Quartus使用EP4CE6E22C8的Final timing model，资源占用：

| 资源 | 使用 |
|---|---:|
| Logic elements | 1189 / 6272（19%） |
| Memory bits | 65536 / 276480（24%） |
| 9-bit multipliers | 10 / 30（33%） |
| PLL | 1 / 2（50%） |

### Quartus警告边界

编译存在预期警告：

- AD9238 CH2引脚为板卡兼容而保留，当前算法未使用；
- DAC数据固定中码，综合器因此报告常量输出；
- 板卡未提供ADC和DAC外部器件的完整input/output delay参数，Timing
  Analyzer报告设计未完全约束；
- PLL转发至现有板卡时钟引脚使用非专用输出路径。

这些警告不影响本次“已分析内部路径TNS=0”的结论，但不能据此宣称
ADC/DAC板级接口已经完成全时序闭合。

## 2. 2026-07-29清理前实机基线

以下结果来自本次交付所基于的G题固件和FPGA实测记录：

- FPGA ID：`0x000004D2`，`online=1`；
- 10k、50k、100k、200k、300k、400k、500kHz单频均稳定识别；
- 题目组合`10.5k + 31.5k + 42kHz`：
  - 输入分量峰峰值为`120/60/40mVpp`；
  - 输出分量峰值为`60/30/20mV`；
  - `Upp=157mV`，`Urms=50mV`；
  - 最大频率误差9Hz；
  - 参数约151ms，3周期波形约291ms。
- `100k + 300k + 400kHz`叠加`1MHz/200mVpp`干扰：
  - 有效分量仍为`60/30/20mV`；
  - `Upp=154mV`，`Urms=49mV`；
  - `spur=none`，`under2s=1`。
- ASCII `1`/`3`周期命令和无信号门限已经实测。

详细串口证据位于：

```text
STM32/Docs/G题无屏联调与标定说明.md
```

## 3. 交付镜像实机复核

本次清理后重新生成了HEX和SOF，但当前环境不能代替操作者完成信号源、
示波器和目标板操作。交接后至少复核：

1. FPGA ID `0x000004D2`；
2. 100kHz单频；
3. 10.5/31.5/42kHz组合；
4. 100/300/400kHz加1MHz干扰；
5. 1/3周期切换；
6. 无信号；
7. 所有帧`under2s=1`。

只有上述烧录后快速回归完成，才能把“交付镜像实机复核”状态改为通过。

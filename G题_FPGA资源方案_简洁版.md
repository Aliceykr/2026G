# G题 FPGA 资源方案（简洁版）

## 1. 推荐总体结构

```text
模拟低通
   ↓
AD9238：50MSPS
   ↓
FPGA整数抽取
   ├─ 5MSPS：波形、峰峰值、真有效值
   └─ 2MSPS：4096点FFT数据
   ↓
公共RAM缓存
   ↓
STM32完成FFT、参数计算和显示
```

---

## 2. FPGA端处理原则

- 删除 DDS 及正弦、方波、三角波 ROM；
- 停用旧 IIR 和 `replay_biquad`；
- 复用现有两个 `2048×16` RAM，组成一个 `4096×16` 公共缓存；
- FFT 放在 STM32 中完成，不在 FPGA 内实现；
- FPGA 只做整数抽取、低阶数字滤波、数据缓存和 SPI 传输；
- 时域模式和 FFT 模式尽量共用同一块 RAM。

---

## 3. 滤波方案

不建议让 EP4CE6 单独承担 50MSPS 下的高阶抗混叠数字滤波。

采用：

```text
模拟低通：承担主要抗混叠和1MHz以上干扰抑制
FPGA数字滤波：使用2～3个SOS进行残余干扰抑制
```

数字滤波建议：

```text
ADC数据：16～18位
滤波系数：18位定点
内部状态：36～40位
输出：16位饱和
```

避免继续使用资源占用较大的 32×32 位乘法结构。

---

## 4. 资源判断

两条降采样支路本身只需要计数器、有效信号和少量寄存器，资源占用很小。

主要资源消耗来自：

- 高阶数字滤波器；
- 32位乘法器；
- 大容量RAM；
- 原工程中的DDS和波形ROM。

因此：

```text
原工程上直接叠加全部新功能：资源和时序风险较大
裁剪成G题专用工程：大概率可以实现
```

---

## 5. 建议的最小验证版本

先实现：

```text
AD9238
   ↓
抽取到5MSPS
   ↓
1个SOS数字滤波器
   ↓
4096×16 RAM
```

然后在 Quartus 中检查：

```text
Total logic elements
Embedded multiplier usage
Total block memory bits
M9K使用数量
Fmax
Setup slack
```

根据真实编译结果，再决定是否增加第二、第三个 SOS。

---

## 6. 最终结论

```text
模拟滤波承担主要抗混叠
FPGA负责低阶数字滤波、抽取和缓存
STM32负责FFT、参数分析和显示
```

这种分工最适合当前 EP4CE6 平台，资源压力和开发风险都相对可控。

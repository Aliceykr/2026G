# 2026 G题构建与烧录说明

最后更新：`2026-07-29 19:21:30 +08:00`

## 1. STM32

工程：

```text
STM32/MDK-ARM/stm32_FilterShell_2026GA.uvprojx
```

命令行构建：

```powershell
& 'E:\100ProgramSoftware\keil5\UV4\UV4.exe' `
  -b '.\STM32\MDK-ARM\stm32_FilterShell_2026GA.uvprojx' `
  -j0 `
  -o '.\STM32\MDK-ARM\keil_build.log'
```

烧录文件：

```text
STM32/Build/stm32_FilterShell_2026GA.hex
```

使用ST-Link按STM32F407ZG常规流程烧录。上电前确认FPGA已经配置，
否则串口会报告`online=0`。

### 主机测试

在`STM32`目录执行：

```powershell
gcc -std=c11 -Wall -Wextra -Werror `
  -I App\Inc -I Tests\HostSpectrum\cmsis_stub `
  Tests\HostSpectrum\test_spectrum.c `
  App\Src\SpectrumAnalyzer.c `
  Tests\HostSpectrum\cmsis_stub\cmsis_fft_stub.c `
  -lm -o test_spectrum.exe

gcc -std=c11 -Wall -Wextra -Werror `
  -I App\Inc -I Tests\HostSpectrum\cmsis_stub `
  Tests\HostMeasurement\test_g_measurement.c `
  App\Src\GMeasurement.c App\Src\GMeasurementCalibration.c `
  App\Src\SpectrumAnalyzer.c `
  Tests\HostSpectrum\cmsis_stub\cmsis_fft_stub.c `
  -lm -o test_g_measurement.exe
```

## 2. FPGA

工程名为`ADC_RAM`，Revision为`ADC_RAM_G`：

```powershell
& 'E:\100ProgramSoftware\Quartus\quartus\bin64\quartus_sh.exe' `
  --flow compile ADC_RAM -c ADC_RAM_G
```

烧录文件：

```text
FPGA/output_files_g/ADC_RAM_G.sof
```

使用USB-Blaster在Quartus Programmer中下载SOF。断电后SOF失效；如需
脱机上电运行，应再按实际配置芯片型号生成并烧录JIC。

### ModelSim

需要编译：

```text
FPGA/src/g_cic3_decimator.v
FPGA/src/g_sos3_lowpass.v
FPGA/Tests/RTL/tb_g_filter_precision.sv
```

运行顶层`tb_g_filter_precision`至`run -all`，必须看到：

```text
PASS: g_filter_precision
```

## 3. 联调

1. 先下载FPGA SOF，再烧录或复位STM32。
2. 串口连接PA9、PA10和GND，电平必须为3.3V TTL。
3. 波特率设置为`115200 8N1`。
4. 确认启动行含`id=0x000004D2,online=1`。
5. 输入`3`，应返回`G_CMD,wave_cycles=3,ok=1`。
6. 输入`1`，应返回`G_CMD,wave_cycles=1,ok=1`。

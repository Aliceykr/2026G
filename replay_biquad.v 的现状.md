### 关于 replay_biquad.v 的现状

`replay_biquad.v` 当前只完成了代码编写和顶层实例化，尚未经过可靠的仿真、上板测试和滤波效果验证，不能视为已调通模块。

本次G题改造中，不应直接复制或扩展该模块。它只能作为以下内容的参考：

- ADC无符号数据转有符号数据；
- S2.30系数格式；
- DF2T二阶节差分方程；
- 64位乘法和定点缩放；
- 饱和处理；
- SPI动态系数配置思路。

请重新设计独立的 `g_sos_engine.v`，并从单个SOS开始建立Testbench，与Python/MATLAB浮点模型逐点对比。确认数值正确、无溢出、无丢样且Quartus时序通过后，再扩展到多级SOS。

不要假定 `replay_biquad.v` 能够稳定运行在50MSPS，也不要将其作为当前方案已证明可用的部分。